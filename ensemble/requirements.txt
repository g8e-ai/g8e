# g8ee - g8e Engine dependencies
#
# Every entry below is a direct import in app/ or tests/. Transitive deps
# (e.g. requests via google-auth, tenacity via google-genai) must NOT be
# pinned here unless the engine code imports them directly.

# HTTP server
fastapi==0.135.2
uvicorn==0.46.0

# Data validation and modeling
pydantic==2.13.4

# Async HTTP and TLS
aiohttp==3.14.3
httpx==0.28.1
cryptography==50.0.1

# Protocol Buffers (runtime parsing + build-time stub generation)
protobuf==7.36.0
grpcio-tools==1.83.0

# g8e Protocol Library
g8e>=1.5.6

# LLM providers
openai==2.36.0
ollama==0.6.2
google-genai==2.20.0
anthropic==1.2.0

# Retry primitives (used directly in app/llm/providers/gemini.py)
tenacity==9.1.4

# Vertex AI Search grounding (Discovery Engine)
google-cloud-discoveryengine==0.19.0

# Test framework
pytest==9.1.1
pytest-cov==7.1.0
pytest-asyncio==1.4.0
pytest-mock==3.15.1
pytest-timeout==2.4.0
pytest-xdist[psutil]==3.8.0

# Lint and static type checking
pyright==1.1.409
ruff==0.16.5

# Documentation
mkdocs>=1.6.1
sentence-transformers>=6.0.0
