# ACME Corp Global Fleet Demo (GENERATED - do not edit by hand)
#
# Batch of devices. See docker-compose.yml for full fleet configuration.
# Regenerate via `make generate-compose` or `./scripts/generate-compose.sh`.

networks:
  acme-net:
    external: true

x-edge-env: &edge-env
  DEVICE_TOKEN: ${DEVICE_TOKEN:-}
  G8E_ENDPOINT: ${G8E_ENDPOINT:-g8e.local}

x-edge-device: &edge-device
  image: acme-edge-device:latest
  networks:
    - acme-net
  # Simulate internet connectivity to the g8e platform via public endpoints.
  # This keeps the "simulated company" isolated on its own network.
  extra_hosts:
    - "g8e.local:host-gateway"
    - "g8ed:host-gateway"
    - "g8es:host-gateway"
  labels:
    - "demo.service=acme-edge"
  volumes:
    - g8es-ssl:/g8es:ro
  restart: unless-stopped

services:
  pos-store-dxb-001:
    <<: *edge-device
    container_name: acme-pos-store-dxb-001
    hostname: pos-store-dxb-001
    environment:
      <<: *edge-env
      DEVICE_NAME: pos-store-dxb-001
      DEVICE_FUNCTION: pos
      DEVICE_SITE: store
      DEVICE_LOCATION: dxb
      DEVICE_PROFILE: healthy

  pos-store-jnb-001:
    <<: *edge-device
    container_name: acme-pos-store-jnb-001
    hostname: pos-store-jnb-001
    environment:
      <<: *edge-env
      DEVICE_NAME: pos-store-jnb-001
      DEVICE_FUNCTION: pos
      DEVICE_SITE: store
      DEVICE_LOCATION: jnb
      DEVICE_PROFILE: healthy

  kiosk-airport-dxb-001:
    <<: *edge-device
    container_name: acme-kiosk-airport-dxb-001
    hostname: kiosk-airport-dxb-001
    environment:
      <<: *edge-env
      DEVICE_NAME: kiosk-airport-dxb-001
      DEVICE_FUNCTION: kiosk
      DEVICE_SITE: airport
      DEVICE_LOCATION: dxb
      DEVICE_PROFILE: healthy

  kiosk-airport-jnb-001:
    <<: *edge-device
    container_name: acme-kiosk-airport-jnb-001
    hostname: kiosk-airport-jnb-001
    environment:
      <<: *edge-env
      DEVICE_NAME: kiosk-airport-jnb-001
      DEVICE_FUNCTION: kiosk
      DEVICE_SITE: airport
      DEVICE_LOCATION: jnb
      DEVICE_PROFILE: healthy

  camera-hq-dxb-001:
    <<: *edge-device
    container_name: acme-camera-hq-dxb-001
    hostname: camera-hq-dxb-001
    environment:
      <<: *edge-env
      DEVICE_NAME: camera-hq-dxb-001
      DEVICE_FUNCTION: camera
      DEVICE_SITE: hq
      DEVICE_LOCATION: dxb
      DEVICE_PROFILE: healthy

  camera-hq-jnb-001:
    <<: *edge-device
    container_name: acme-camera-hq-jnb-001
    hostname: camera-hq-jnb-001
    environment:
      <<: *edge-env
      DEVICE_NAME: camera-hq-jnb-001
      DEVICE_FUNCTION: camera
      DEVICE_SITE: hq
      DEVICE_LOCATION: jnb
      DEVICE_PROFILE: healthy

  printer-office-dxb-001:
    <<: *edge-device
    container_name: acme-printer-office-dxb-001
    hostname: printer-office-dxb-001
    environment:
      <<: *edge-env
      DEVICE_NAME: printer-office-dxb-001
      DEVICE_FUNCTION: printer
      DEVICE_SITE: office
      DEVICE_LOCATION: dxb
      DEVICE_PROFILE: healthy

  printer-office-jnb-001:
    <<: *edge-device
    container_name: acme-printer-office-jnb-001
    hostname: printer-office-jnb-001
    environment:
      <<: *edge-env
      DEVICE_NAME: printer-office-jnb-001
      DEVICE_FUNCTION: printer
      DEVICE_SITE: office
      DEVICE_LOCATION: jnb
      DEVICE_PROFILE: healthy

  sensor-factory-dxb-001:
    <<: *edge-device
    container_name: acme-sensor-factory-dxb-001
    hostname: sensor-factory-dxb-001
    environment:
      <<: *edge-env
      DEVICE_NAME: sensor-factory-dxb-001
      DEVICE_FUNCTION: sensor
      DEVICE_SITE: factory
      DEVICE_LOCATION: dxb
      DEVICE_PROFILE: healthy

  sensor-factory-jnb-001:
    <<: *edge-device
    container_name: acme-sensor-factory-jnb-001
    hostname: sensor-factory-jnb-001
    environment:
      <<: *edge-env
      DEVICE_NAME: sensor-factory-jnb-001
      DEVICE_FUNCTION: sensor
      DEVICE_SITE: factory
      DEVICE_LOCATION: jnb
      DEVICE_PROFILE: healthy

  controller-factory-dxb-001:
    <<: *edge-device
    container_name: acme-controller-factory-dxb-001
    hostname: controller-factory-dxb-001
    environment:
      <<: *edge-env
      DEVICE_NAME: controller-factory-dxb-001
      DEVICE_FUNCTION: controller
      DEVICE_SITE: factory
      DEVICE_LOCATION: dxb
      DEVICE_PROFILE: healthy

  controller-factory-jnb-001:
    <<: *edge-device
    container_name: acme-controller-factory-jnb-001
    hostname: controller-factory-jnb-001
    environment:
      <<: *edge-env
      DEVICE_NAME: controller-factory-jnb-001
      DEVICE_FUNCTION: controller
      DEVICE_SITE: factory
      DEVICE_LOCATION: jnb
      DEVICE_PROFILE: healthy


volumes:
  g8es-ssl:
    external: true
