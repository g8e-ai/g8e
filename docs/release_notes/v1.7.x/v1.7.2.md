## [1.7.2] - 2026-08-13

### Overview

CLI enrollment refactor. This release replaces the scattered, multi-function
CLI enrollment path (`BootstrapWithURL`/`CLIEnroll`/`ReEnroll`/
`EnrollWithGateway`/`AutoRenewCertificate`) with a single
`EnrollmentCoordinator` that owns the enrollment state machine, adds a
human-approved CLI recovery flow for new CLIs against an existing gateway,
adds an mTLS-protected CLI certificate rotation endpoint, builds in OS trust
installation before browser launch, removes the `--tpm` flag (file-backed
EC P-256 keys on all platforms), and removes the legacy per-platform
trust-install script routes. This is a **breaking change** for any client
that called the removed `/api/v1/auth/cli/enroll` endpoint or fetched the
removed trust-install scripts.

The release also fixes a stale-trust-bundle bug on the reused-identity
enrollment path: after `gw clean` regenerated the gateway PKI, a workstation
with a complete identity from the old gateway failed `auth enroll` with a
raw TLS verification error and no diagnosable cause. The coordinator now
fetches the live gateway root CA from the unauthenticated discovery endpoint
before the reuse decision and routes a stale-bundle identity to the recovery
flow automatically (see "Added" / "Changed" below).

### Breaking

* **`/api/v1/auth/cli/enroll` (`handleCLIEnrollment`) removed** — The
  legacy CLI enrollment endpoint is gone. CLI enrollment is now driven
  client-side by the `EnrollmentCoordinator`, which calls the bootstrap,
  recovery, and rotation endpoints as needed. The `constants.APIPaths.
  AuthCLIEnroll` constant is removed. External clients that called this
  endpoint must migrate to the recovery flow (for new CLIs against an
  existing gateway) or the bootstrap endpoint (for the first CLI on a
  fresh gateway).
* **Per-platform trust-install script routes removed** —
  `/web-cert.sh`, `/web-cert.ps1`, and
  `/.well-known/g8e/pki/trust-windows` are gone. OS trust installation
  is now performed client-side by the `EnrollmentCoordinator` via
  `platform.SystemTrustInstaller` before browser launch. The
  `constants.APIPaths` constants for these routes are removed.
* **`--tpm` flag removed** — All platforms now use file-backed EC P-256
  CLI keys. The `--tpm` flag and its associated `KeyProvider` build-tag
  machinery are removed. The `KeyProvider` interface remains as the
  extension point for future non-exportable platform keys (TPM/CNG).

### Added

* **`EnrollmentCoordinator`** (`internal/cli/auth/enrollment.go`) —
  Single owner of the CLI enrollment state machine. Decides whether to
  bootstrap, recover, rotate, or reuse the local CLI identity based on
  `CredentialStore.Inspect` classification (absent/complete/partial/
  corrupt). Never writes to stdout/stderr directly (uses `OutputFunc`),
  never opens a browser except via `BrowserOpener`/`PasskeyRegistrar`,
  never invokes sudo except via `SystemTrustInstaller`. Injectable
  dependencies via `EnrollmentCoordinatorDeps` (nil fields get
  production defaults). The command layer's `enrollCoordinatorFactory`
  package-level var returns an `Enroller` interface for test mocking.
* **`CredentialStore`** (`internal/cli/auth/credential_store.go`) —
  Typed API over local CLI identity files. `Inspect` classifies local
  state as a set. `Stage`/`Commit` write a new identity atomically
  (credentials last). `Rollback` releases staged state. `Clear` removes
  local credentials for logout/recovery but retains the shared OS root
  CA (§4.3 ownership). Concurrent-safe via `sync.Mutex`.
* **CLI recovery flow** — New human-approved recovery for enrolling a
  new CLI against an already-bootstrapped gateway. Endpoints:
  `POST /api/v1/auth/cli/recovery/request` (public, CSR is
  proof-of-possession anchor),
  `GET /api/v1/auth/cli/recovery/status` (public, opaque token lookup),
  `POST /api/v1/auth/cli/recovery/approve` (web-session protected,
  existing user authorizes),
  `POST /api/v1/auth/cli/recovery/complete` (public, requires token +
  CSR private-key signature). Gateway-side:
  `CLIRecoveryController` (`cli_recovery_controller.go`),
  `CLIRecoveryService` (`cli_recovery_service.go`). Token is hashed
  (SHA-256) at rest; state transitions are atomic via
  `DocConditionalUpdate`; request is bound to CSR public-key
  fingerprint.
* **CLI rotation endpoint** — `POST /api/v1/auth/cli/rotate`
  (mTLS-protected, `RouteAuthMTLS`). Identity derived from verified
  mTLS certificate URI SAN — request body fields are NOT trusted.
  Single transactional replacement: new cert signed before old session
  deactivated, old cert revoked after session replacement commits.
  Gateway-side: `CLIRotationController`
  (`cli_rotation_controller.go`). Registered on `buildPublicRouter`
  only, never on plain HTTP.
* **Built-in OS trust installation** — `auth enroll` installs the
  gateway root CA into the OS trust store before opening the browser
  for the passkey ceremony. `--no-system-trust` skips this step
  (administrator-managed trust opt-out, NOT a passkey bypass). System
  trust failure stops before browser launch. After installation or
  stale anchor removal, the user is directed to close all open browser
  windows before clicking the enrollment link so the browser picks up
  the trust store change.
* **Stale trust anchor detection and removal** — Before installing the
  new root CA, `auth enroll` enumerates existing g8e root CA anchors in
  the OS trust store that do not match the current gateway's root
  fingerprint (orphaned anchors from a previous gateway instance, e.g.,
  after `gw clean` regenerated the CA). If stale anchors are found, the
  user is prompted to confirm removal; declining aborts enrollment
  before browser launch. On confirmation, the stale anchors are removed
  and the trust bundle is refreshed. `platform.SystemTrustInstaller`
  exposes `ListStaleAnchors` and `RemoveStaleAnchors` methods with
  per-platform implementations (Linux: `g8e-root-*.crt` file scan,
  Windows: `LocalMachine\Root` CN filter, Darwin: keychain CN filter).
  The `EnrollmentCoordinator` gains a `ConfirmFunc` dependency (auto-
  confirm default for internal callers; stdin-reading impl injected by
  the interactive `auth enroll` command).
* **`gw clean` removes OS trust anchors** — `gw clean` now removes g8e
  root CA anchors from the OS trust store before wiping the runtime
  directory. An empty keep-fingerprint is passed to `ListStaleAnchors`
  (after clean there is no "current" anchor), so every g8e anchor is
  listed and removed. This runs before the runtime wipe so that, if OS
  cleanup fails with an elevation error, the user can retry while the
  runtime state is still intact. Best-effort: on
  `ErrSystemTrustUnsupported` (stub platform) or any trust-store error,
  the runtime wipe proceeds (the runtime wipe is the destructive
  primary action). The confirmation prompt gained a fifth warning line:
  "Remove g8e root CA anchors from the OS trust store."
* **`EnrollmentClient`** (`internal/cli/auth/enrollment_client.go`) —
  Gateway enrollment transport. HTTP I/O only: receives
  `context.Context`, returns typed `EnrollmentArtifacts`, writes no
  files, performs no UI/platform work. Satisfies `EnrollmentGateway`
  interface. Replaces the old transport slices. Gains
  `DiscoverGatewayCA` (fetches the live gateway root CA bundle from
  `/.well-known/g8e/pki/ca-bundle` over plain HTTP and derives the
  SHA-256 fingerprint locally via `platform.ExtractRootAnchors`/
  `CertFingerprint`; best-effort, no fingerprint pin).
* **Live gateway CA discovery step** — Before the state-machine switch,
  `EnrollmentCoordinator.Enroll` fetches the live gateway root CA
  bundle from the unauthenticated discovery surface
  (`GET /.well-known/g8e/pki/ca-bundle`, `RouteAuthNone`, plain HTTP)
  and derives its SHA-256 fingerprint. The call is best-effort: on
  network failure the coordinator falls through to the existing state
  machine (offline reuse, absent identity → bootstrap fails with a
  clear network error) and prints a diagnostic warning when reuse is
  attempted. Discovery runs unconditionally at the top of `Enroll` so
  the live fingerprint is available for all paths, but only the
  complete-reuse path uses it for routing.
* **`LocalIdentity.BundleStale` field** — Set by the coordinator (NOT
  by `CredentialStore.Inspect`, which has no gateway access) when the
  local trust bundle's primary root fingerprint does not match the
  live gateway root fingerprint on a `Complete` identity. Routes the
  identity to the recovery flow instead of reuse. `Inspect` and
  `LocalIdentity` godocs now explicitly document the local-only
  consistency boundary: `State` reflects local file consistency only;
  gateway liveness is a coordinator-level concern.
* **`FileKeyProvider`** (`internal/cli/auth/key_provider.go`) —
  File-backed EC P-256 key generation on all platforms. Implements
  `KeyProvider` interface.
* **`PasskeyRegistrar` interface + hardened `passkeyRegistrar`** —
  Browser-based passkey registration ceremony. Prepares SSE listener
  before browser launch, uses `since_id=0` cursor for live-only
  events, filters by type/user/session, surfaces browser-open errors,
  propagates context cancellation.
* **`CLIRecoveryControllerDeps` / `CLIRotationControllerDeps`** —
  Per-controller `Deps` structs for the two new gateway controllers,
  following the v1.7.1 decomposition pattern.

### Changed

* **`auth enroll` behavior** — Re-running `auth enroll` with a
  healthy identity now reuses it (no new certificate, no privilege
  prompt). Use `--rotate-cli` to force rotation. Partial/corrupt
  local state triggers the recovery flow, not a silent overwrite.
  Expiring certificates (within 24h of expiry) are auto-rotated.
* **Stale-bundle-on-reuse routing** — A `Complete` identity whose
  local trust bundle does not match the live gateway root CA (e.g.,
  after `gw clean` regenerated the gateway PKI) now routes to the
  recovery flow instead of reuse. The old CLI cert was issued by the
  old CA and cannot authenticate to the new gateway via mTLS, so
  rotation is impossible; recovery issues a fresh cert signed by the
  new CA over plain HTTP. Previously this case failed with a raw
  `x509: certificate signed by unknown authority` TLS error during
  the passkey ceremony with no diagnosable cause.
* **`installSystemTrust` live-fingerprint source** — The "active"
  fingerprint used to filter the stale-anchor list now comes from the
  live gateway (via discovery), not the local bundle. Using the local
  bundle was the original bug on the reused-identity path: the local
  bundle and the OS store are stale in lockstep, so they agreed with
  each other and the detector saw nothing wrong. On the new-enrollment
  paths (bootstrap/recovery/rotation) the artifacts' bundle IS the
  live bundle, so behavior is unchanged. When discovery is
  unreachable, the coordinator falls back to the bundle's own
  fingerprint (preserving pre-discovery behavior) after printing a
  diagnostic warning. The reused-identity path also refreshes the
  local trust bundle from the live gateway when intermediates differ
  but the root is unchanged. The `currentFingerprint` variable was
  renamed to `keepFingerprint` with a godoc stating it must come from
  the live gateway. The multi-purpose `ensureSystemTrust` was
  rewritten as `installSystemTrust`, an explicit composition of
  single-purpose platform methods (`ExtractRootAnchors`,
  `VerifyRootUsable`, `IsTrusted`, `InstallRoot`, `ListStaleAnchors`,
  `RemoveStaleAnchors`). The deleted `EnsureSystemTrust` /
  `SystemTrustResult` / `SystemTrustStatus` /
  `SystemTrustAlreadyTrusted` / `SystemTrustInstalled` types are
  replaced by `IsTrusted(ctx, fingerprint) (bool, error)` (read-only)
  and `InstallRoot(ctx, root, fingerprint) error` (write-only).
* **`--no-system-trust` stale detection** — Stale-anchor detection
  now runs under `--no-system-trust`; only the installation step is
  skipped. The user may have stale anchors from a previous gateway
  that break the browser even when the CLI skips installation. When
  stale anchors are removed under `--no-system-trust`, the browser-
  close directive is printed but no new anchor is installed. This is
  a behavior change — documented in the `--no-system-trust` flag help
  and the troubleshooting guide.
* **`auth logout` behavior** — Removes local CLI credential material
  (CLI cert, CLI key, credentials JSON) but does NOT remove the
  shared OS root CA. `CredentialStore.Clear` no longer calls
  `DeleteCredentials` (which removed the trust bundle). This was a
  real bug caught by `TestLogoutCmd_OSRootCARetained`.
* **`mcp stdio` vs `mcp agent run`** — `mcp stdio` is a credential
  consumer only; it loads credentials and builds the mTLS connection,
  never invoking the enrollment coordinator. `mcp agent run` runs the
  interactive `EnrollmentCoordinator` with default
  `EnrollmentOptions{}`.
* **`gwstdout.go`** — `auth enroll` blurb updated to mention built-in
  OS trust installation and `--no-system-trust`.
* **`HTTPHandler` controller count** — Now 18 controller fields (was
  16 in v1.7.1): added `CLIRecoveryController` and
  `CLIRotationController`.

### Removed

* **`handleCLIEnrollment` route/handler/constant** — Replaced by the
  client-side `EnrollmentCoordinator` + recovery/rotation endpoints.
* **`/web-cert.sh`, `/web-cert.ps1`, `/.well-known/g8e/pki/trust-windows`
  routes** — Replaced by client-side `SystemTrustInstaller`.
* **`BootstrapWithURL`, `CLIEnroll`, `ReEnroll`, `EnrollWithGateway`,
  `AutoRenewCertificate`, `CheckBootstrapStatus` (enrollment transport
  functions)** — Replaced by `EnrollmentClient` methods.
* **`--tpm` flag** — File-backed EC P-256 keys on all platforms.
* **`auth_integration_test.go`** — Deleted (stale `ReEnroll` comments,
  skipped test asserting nothing at runtime).

### Documentation

* **`docs/architecture/auth.md`** — Enrollment state machine (4
  states), recovery flow, rotation flow, system trust installation,
  logout ownership policy, practical flows. Updated for v1.7.2 to
  document the live gateway CA discovery step, the `BundleStale`
  state and recovery-on-stale-bundle routing, the live-fingerprint
  source for stale-anchor detection, and the `--no-system-trust`
  stale-detection behavior change.
* **`docs/architecture/network.md`** — CLI recovery/rotation routes
  table, removed old script-route references, updated HTTP surface
  description.
* **`docs/architecture/gateway.md`** — Bootstrap HTTP router, public
  HTTPS router route categories, route auth classification table for
  CLI enrollment routes.
* **`docs/guides/`** — `air_gap.md` (HTTP surface),
  `getting_started.md` (trust installation),
  `connect_operator_to_gateway.md` (rotation/reuse),
  `connect_apps_to_gateway.md` (HTTP surface + cert renewal),
  `build_apps.md` (trust installation).
* **`docs/devs/codemap.md`** — Updated `PKIController`,
  `BootstrapController`, `buildHTTPRouter`, `buildPublicRouter`
  one-liners; added `CLIRecoveryController`,
  `CLIRotationController`, `CLIRecoveryService` entries; added "CLI
  Auth Package (Enrollment Coordinator)" section with caller graph
  and component descriptions; updated `CLISessionService` tree entry.
* **`docs/devs/troubleshooting.md`** — CLI recovery flow,
  `--no-system-trust` flag, certificate expiry and rotation, logout
  retains OS root CA, Windows file-backed keys. Updated for v1.7.2:
  the "Stale trust bundle after PKI regeneration" entry now describes
  the automatic recovery routing (re-run `auth enroll` instead of
  `auth logout && auth enroll` in the common case), and the
  `--no-system-trust` entry documents that stale-anchor detection
  still runs.
* **`docs/devs/tests.md`** — CLI enrollment coordinator tests section
  (coordinator-level, CredentialStore, command-layer, gateway-side
  recovery/rotation); updated HTTP port description (removed
  `/enroll`).
* **`protocol/docs/constants.md`** — Already consistent (recovery/
  rotation paths present, `AuthCLIEnroll` and script routes absent).

### Tests

* **Coordinator tests** (`enrollment_coordinator_test.go`) — Full
  state machine: healthy reuse, partial→recovery, expired→rotation,
  absent→bootstrap, `--no-system-trust` skip, system trust failure
  stops before browser, `--rotate-cli` forces rotation, stale anchor
  detection/confirmation/removal, user-declined stale removal aborts,
  stale list error best-effort proceed, unsupported platform skip.
  Updated for v1.7.2 to inject matching live fingerprints via
  `mockGateway.DiscoverGatewayCA` and assert `--no-system-trust`
  still runs stale detection. New tests:
  `TestEnroll_ReusedIdentity_StaleBundle_RoutesToRecovery`,
  `TestEnroll_ReusedIdentity_HealthyBundle_ReusesWithLiveFingerprint`,
  `TestEnroll_ReusedIdentity_DiscoveryUnreachable_WarnsAndProceeds`,
  `TestEnroll_ReusedIdentity_StaleBundle_DetectsStaleOSAnchors`,
  `TestEnroll_NoSystemTrust_StillRunsStaleDetection`,
  `TestEnroll_Bootstrap_DiscoveryMatchesArtifacts`. New helpers:
  `writeCompleteIdentityWithBundleFP`; new mock fields
  `discoveryBundlePEM`/`discoveryFingerprint`/`discoveryErr`/
  `discoveryCalls` on `mockGateway` and `lastStaleFingerprint` on
  `mockTrustInstaller`.
* **CredentialStore tests** (`credential_store_test.go`) — 5 tests:
  interrupted-commit retry, rollback, permissions (0600), concurrent
  no-torn-state (race-clean), Clear retains trust bundle.
* **Command-layer tests** (`auth_enroll_test.go`) — Option
  propagation, coordinator error propagation, healthy reused identity
  no rotate, `--rotate-cli` wiring, `--no-system-trust` wiring,
  system trust installed output, logout retains OS root CA, `mcp
  stdio` does not invoke enrollment.
* **Gateway tests** — `cli_recovery_controller_test.go`,
  `cli_recovery_service_test.go`,
  `cli_rotation_controller_test.go`; route removal assertions
  (`TestRemovedCLIEnrollRoute`,
  `TestRouteAuthRegistry_RotationAndRemovedEnroll`).
* All test suites pass: `go test`, `go test -race`,
  `go test -tags=integration`, cross-platform `go build`
  (linux/darwin/windows), `gofmt` clean.
