## [2.1.8] - 2026-09-09

### Overview

v2.1.8 replaces the multi-command, multi-flag browser frontend setup with one guided Gateway workflow. `./g8e gw connect <frontend-origin>` validates and normalizes the origin, derives the safest WebAuthn configuration, starts or deliberately restarts the Gateway, establishes local certificate trust with explicit consent, verifies HTTPS and CORS against the running process, and prints a short frontend prompt. The obsolete `auth enroll gui` command family is removed. Restart preserves the complete prior launch configuration through a versioned launch profile rather than posture only.

### Added

* **`gw connect` guided command** — `./g8e gw connect <frontend-origin>` is the sole local browser connection workflow. It accepts one positional origin, validates it through the centralized browser origin parser, derives an exact-host RP ID, starts or restarts the Gateway with the correct CORS and passkey settings, installs local certificate trust with explicit consent, verifies HTTPS and CORS against the running process, and prints a concise frontend prompt. MVP flags: `--passkey-rp-id`, `--passkey-rp-name`, `--no-system-trust`, `--no-open`, `--yes`.
* **Centralized browser origin validation** — `internal/cli/browserorigin` provides a typed `Origin` struct and pure `Parse` and `ValidateRPID` functions. Both the gateway wizard and `gw connect` consume this implementation. `Parse` enforces HTTPS for non-loopback hosts, rejects paths, queries, fragments, userinfo, and non-loopback IP hosts, canonicalizes scheme and hostname to lowercase, canonicalizes Unicode hostnames to IDNA ASCII via `golang.org/x/net/idna`, strips trailing slashes, preserves explicit non-default ports, and derives the RP ID from `Hostname` never `Host`. `ValidateRPID` accepts an exact-host match or a registrable parent suffix, canonicalizes the override to IDNA ASCII + lowercase, returns the canonical form, and rejects public suffixes via `golang.org/x/net/publicsuffix`.
* **Versioned launch profile persistence** — `internal/cli/serve/launch_profile.go` persists the complete validated `GatewayConfig` to `.g8e/pids/operator-launch-profile.json` with private permissions through `RuntimeFileService`. `GatewayConfig` uses explicit snake-case JSON tags for a stable wire shape. `ReadLaunchProfile` uses `DisallowUnknownFields` and trailing-JSON detection so multiple concatenated JSON values are rejected as corrupted. `WriteLaunchProfile` calls `ValidateLaunchProfile` before serializing, so invalid configs are rejected at the write boundary without touching the filesystem. `ValidateLaunchProfile` checks posture, port range (0-65535), port collision, rate limits, network identity file, log level, certificate mode, browser origins, RP/origin relationships, and service URLs (public/consensus/mcp/a2a). Every successful background `gw start` writes the profile. `gw restart` reads and validates the complete profile and re-runs network identity detection before starting the child. Missing, malformed, unknown-version, or unsafe profiles fail closed.
* **Reusable trust discovery and inspection** — `internal/cli/auth/trust_discovery.go` extracts `ValidateTrustBundle` and `DiscoverLiveTrustBundle` from identity enrollment so both `gw connect` and identity enrollment use the same root selection, chain verification, and fingerprint formatting. `internal/cli/auth/trust_state.go` adds `InspectTrustState` for read-only trust-state inspection.
* **HTTPS and CORS verifier** — `internal/cli/frontendverify/verifier.go` is a read-only verifier with an injectable HTTP client factory. It builds a TLS client from the discovered and validated g8e root, never uses `InsecureSkipVerify`, and returns typed ordered verification results for HTTPS health, certificate chain, CORS origin, CORS credentials, CORS methods, CORS headers, and CORS Vary (requiring `Vary: Origin`).
* **Typed connection error constants** — `ErrLaunchProfileMissing`, `ErrLaunchProfileCorrupted`, `ErrLaunchProfileVersionUnsupported`, `ErrLaunchProfileInvalid`, `ErrHTTPSCertificateVerification`, `ErrCORSPreflightRejected`, `ErrManualBrowserTrustRequired`, `ErrGatewayReadinessTimeout`, `ErrGatewayConfigMismatch`, `ErrManagedRestartDeclined` added to `internal/constants/errors.go`.

### Changed

* **`gw restart` restores complete launch configuration** — Restart reads the complete launch profile, re-runs `detectIdentity` for fresh network identity state, and starts the child with the full persisted configuration. Posture-only persistence (`writePosture`/`readPosture`) is removed from `internal/cli/platform/process.go`. Missing or malformed profiles fail closed with typed errors instead of falling back to doctrine defaults.
* **Wizard and GUI origin validation migrated** — `internal/cli/wizard/validate.go` and the former `internal/cli/cmd/gui.go` delegate to `browserorigin.Parse` and `browserorigin.ValidateRPID`. The wizard now rejects non-loopback HTTP origins, non-loopback IP hosts, and public-suffix RP IDs the previous package-private implementation accepted.
* **`golang.org/x/net` promoted to direct dependency** — The module was already in the transitive closure; it was promoted from indirect to direct and vendored to provide `golang.org/x/net/publicsuffix` for registrable-domain validation.
* **Identity enrollment trust extraction** — `EnrollmentCoordinator.installSystemTrust` delegates to `ValidateTrustBundle` instead of calling `platform.ExtractRootAnchors`, `platform.VerifyRootUsable`, and `platform.CertFingerprint` directly. No enrollment behavior changes; the same validation runs in the same order with the same error constants.
* **Tunnel create next steps** — `internal/cli/cmd/tunnel.go` points users to `gw connect` instead of the removed `auth enroll gui` command.
* **Managed process start returns resolved configuration** — `StartOperator` takes a pointer to `OperatorStartOptions` and mutates resolved ports and paths in place. Callers that read `startOpts.GatewayConfig` after start see the resolved values. `gw start`, `gw restart`, `gw connect`, and the MCP auto-start path consume the resolved values. MCP auto-start now writes a launch profile and probes its resolved HTTP port rather than the global default.
* **Failed-start child termination and stale-PID removal** — `stopFailedStart` terminates and reaps the child on PID-write failure, log-close failure, and health-check timeout, joining kill+wait+deletePID errors via `errors.Join`. No failed-start path leaves a live child or stale PID file.
* **Profile-write failure stops untracked gateway** — When `WriteLaunchProfile` fails after a successful process start, `gw connect` stops the untracked gateway and reports both errors. Restart attempts rollback to the previous profile on start failure and on profile-write failure, reporting both the primary failure and rollback result.
* **Discovery and verification URLs use resolved ports** — `gw connect` constructs discovery and HTTPS verification URLs from the active launch profile's resolved `HTTPPort`/`HTTPSPort`, not from global CLI defaults. A Gateway shifted to another port starts successfully and verifies against the correct endpoint.

### Removed

* **`auth enroll gui` command family** — `guiEnrollCmd`, `guiShowCmd`, `guiVerifyCmd`, `guiRemoveCmd`, and all associated models, file I/O, snippet generation, and the broken plain-HTTP CORS checker are deleted. No compatibility alias or shim remains. Pre-existing `.g8e/gui_enrollments.json` files are inert and untouched; they persist until removed through the existing explicit runtime-tree cleanup workflow (`gw clean`).

### Security

* **Exact-origin CORS only** — No wildcard fallback. `Access-Control-Allow-Credentials` remains paired with an exact reflected origin.
* **Exact-host RP ID by default** — The CLI derives the exact frontend hostname as the RP ID, never the shared parent domain. An advanced `--passkey-rp-id` override is validated as either the exact origin hostname or a valid parent suffix; public suffixes and unrelated domains are rejected.
* **No insecure TLS bypass** — The HTTPS and CORS verifier builds a TLS client from the discovered and validated g8e root pool. `InsecureSkipVerify` is never used in production or test acceptance paths.
* **Fail-closed restart** — Missing, malformed, unknown-version, or unsafe launch profiles prevent restart. The CLI never stops a running Gateway when its complete launch configuration is unavailable.
* **Separate consent boundaries** — Trust installation and stale-anchor removal each require explicit consent. `--yes` auto-approves restart only; it does not suppress trust or stale-anchor consent.
* **Launch profiles contain no secrets** — No private keys, tokens, passwords, cookie values, or environment snapshots are persisted. Ephemeral `NetworkIdentityFile` is cleared before serialization and rejected on read.
* **Fail-closed unsupported trust** — Unsupported OS trust installation fails closed with `ErrManualBrowserTrustRequired` unless `--no-system-trust` was explicitly selected. The CLI never silently downgrades to manual trust.
* **Browser-restart gate enforced** — Declining the browser-restart gate after trust changes stops handoff. The CLI never proceeds past a blocking trust-change gate.
* **IDNA canonicalization** — Unicode hostnames and RP ID overrides are canonicalized to IDNA ASCII before persistence and validation, so `gw connect` stores `xn--caf-dma.lovable.app` rather than the caller's original mixed-case or Unicode form.
* **`Vary: Origin` required** — The verifier enforces `Vary: Origin` as a seventh CORS check, preventing cached responses from leaking across origins.

### Documentation

* `docs/guides/lovable.md` rewritten to the one-command `gw connect` journey with the paste prompt, top-level-tab guidance, browser limitation documentation (Local Network Access and third-party cookies), and troubleshooting keyed to `gw connect` output.
* `docs/guides/build_frontend.md`, `docs/guides/connect_frontend_to_gateway.md`, and `docs/guides/connect_apps_to_gateway.md` updated to remove all `auth enroll gui` references, point to `gw connect` for the local case, retain advanced `gw start` flags for multi-origin and public deployments, and document third-party-cookie limitations.
* `docs/guides/cloudflare_tunnel.md` points to `gw connect` for the local case and retains tunnel-specific configuration for public access.
* `demos/README.md` adds a Browser Frontend Connection section pointing to `gw connect` for same-host browser frontend connection.

### Tests

* Phase 0 regression tests documented four root UX failures (GUI origin validator accepts paths and derives port-bearing RP IDs, CORS checker probes plain HTTP, `gw restart` loses non-posture settings, `gw start` returns success when already running without applying new flags). These tests were removed with the GUI command in Phase 5.
* `internal/cli/browserorigin/origin_test.go` — 29 table-driven tests (Tier 1) covering HTTPS, loopback HTTP, ports, trailing slash, mixed-case, IDNA canonicalization, paths, queries, fragments, userinfo, IPv4/IPv6, RP ID exact match, parent suffix, public suffix rejection.
* `internal/cli/serve/launch_profile_validation_test.go` — 20 table-driven tests (Tier 1) covering port range, port collision, log level, cert mode, invalid allowed/passkey origins, RP/origin mismatch, invalid service URLs, fragment/userinfo in URLs, valid-acceptance cases, and write-time validation rejection without writing.
* `internal/cli/serve/launch_profile_test.go` — 18 table-driven tests (Tier 2) covering round-trip, permissions, missing, corrupted, unknown version, invalid posture, negative ports, negative rate limits, stale network identity, trailing-JSON rejection, delete, exists, valid postures, zero ports.
* `internal/cli/auth/trust_discovery_test.go` — 10 tests (Tier 1) covering pure bundle validation: valid bundle, empty, malformed, expired, no root, no chain, fingerprint, invalid content, root-only, chain failure.
* `internal/cli/auth/trust_discovery_integration_test.go` — 6 tests (Tier 2) covering live discovery fetch + validation and network failure.
* `internal/cli/auth/trust_state_test.go` — 6 tests (Tier 1) covering trusted, not trusted, unsupported platform, real errors.
* `internal/cli/frontendverify/verifier_test.go` — 14 tests (Tier 2) with real temporary PKI and HTTPS, never `InsecureSkipVerify`, including `Vary: Origin` enforcement.
* `internal/cli/cmd/gateway_connect_config_match_test.go` — 9 tests (Tier 1) covering `deriveConnectConfig` and `browserConfigMatches` including RP-name comparison.
* `internal/cli/cmd/gateway_connect_render_test.go` — 11 tests (Tier 1) covering rendering functions and URL construction.
* `internal/cli/cmd/gateway_connect_state_machine_test.go` — 16 tests (Tier 2) covering state machine transitions: invalid origin, invalid RP ID, no-args, running-without-profile fail-closed, matching-config proceeds, differing-config declined restart, differing-config `--yes` attempts restart, stopped-gateway attempts start, trust-declined, `--no-system-trust` manual trust, trust-install-with-consent, stale-anchor removal, unsupported-system-trust fail-closed, declined browser-restart gate, verification failure, stopped-gateway `--no-system-trust` proceeds to manual trust, IDNA RP override canonicalized in profile.
* `internal/cli/cmd/gateway_connect_process_lifecycle_integration_test.go` — 3 tests (Tier 2) covering profile-write failure stops gateway, restart rollback to previous config, and resolved-port persistence.
* `internal/cli/cmd/gateway_connect_restart_integration_test.go` — 1 test (Tier 2) covering restart with `--yes` against a real subprocess.
* `internal/cli/cmd/factory_error_test.go` — `TestGatewayConnectCmdWithConfig_FileSvcFactoryError` (Tier 1) asserting `ErrFileServiceInit` wrapping and downstream dependencies not called.
* `internal/cli/platform/process_start_failure_integration_test.go` — 2 tests (Tier 2) covering PID-write failure terminates child and leaves no PID file, and process death during health check removes PID file.
* `./g8e test unit`, `./g8e test integration`, `./g8e test lint`, and `./g8e test coverage` pass. Coverage is 76.2% (threshold 75%). `./g8e test e2e` requires owner credentials (`./g8e auth login`) and is a human acceptance gate.

### Deferred

* **Clipboard integration** — The prompt is always printed. An explicit `--copy` option is deferred until supported terminals and desktop environments are tested.
* **Browser and OS acceptance evidence** — Real Chromium top-level-tab behavior, Local Network Access allow/deny, WebAuthn, cookies, SSE, and Linux/macOS/Windows trust installation remain explicit human acceptance evidence.
* **Compliance evidence** — Per-release compliance evidence generation is a release-owner step that requires a running deployment with current demo evidence.

### Observability frontend (prompt-built, generator-neutral)

v2.1.8 adds a prompt-built, generator-neutral observability frontend stack: typed agent and run state producers, a browser-scoped read-only observe API, an audited browser adapter, and a deterministic contract pack for builder-generated SPAs. The work is split into automated evidence (verified by tests) and owner-operated gates (requiring real browsers, authenticators, and hosted origins).

#### Automated evidence

* **Producer protocol and models (Packet 1)** — Typed `ObserveProducerResponse`, `ObserveProducerAgentStateRequest`, and `ObserveProducerRunStateRequest` across Go, `protocol/models/observe_api.json`, and `protocol/python/g8e/models/observe_api.py` with `extra="forbid"` unknown-field rejection. The ensemble imports producer wire types via aliases from the protocol package. Strict JSON decoding at the Gateway boundary rejects unknown fields and trailing JSON. Focused Gateway validators enforce the supported schema version, required display/role/run-kind fields, recognized lifecycle statuses even on first write, non-negative task counters, completed-not-exceeding-total, and coherent start/end times. Distinct `ErrObserveAgentNotFound` and `ErrObserveRunNotFound` sentinels map to the same non-disclosing 403 response. Cross-language contract tests, Gateway validator tests, ownership tests, and Python model tests pass.
* **Ensemble producer boundary (Packet 2)** — `G8eClientProtocol` and `EventServiceProtocol` declare typed `push_agent_state`/`push_run_state` and `publish_agent_state`/`publish_run_state`. `EventService` implements best-effort publish methods that skip targetless requests, catch exceptions except `CancelledError`, log a warning, and return without raising. Pure identity helpers in `app/services/observe/identity.py` construct deterministic agent IDs (`f"{user_id}:{persona_id}"`) from the persona registry. 36 new tests pass.
* **Lifecycle wiring (Packet 3)** — Chat/tool, consensus, and investigation lifecycle transitions push typed projections through the Packet 2 boundary. Agent states: running, waiting, failed, idle, completed. Run states: queued, running, completed. A multi-turn investigation never attempts terminal-to-running after an ordinary completed chat turn. First-round consensus retry and recoverable tool failure do not terminate the investigation. Task lifecycle is documented as unsupported (no task document creation path exists in the ensemble). Operator dispatch is documented as unsupported (no agent persona projection). 41 new tests pass.
* **Gateway controller and endpoint coverage (Packet 4)** — 36 controller unit tests and 22 endpoint integration tests through the real HTTP router with mTLS app+user SAN certs, real web sessions, browser-scoped observe reads, and durable SSE storage assertions. 2 failed-persistence HTTP tests prove the controller and HTTP path preserve the persist-before-publish invariant. Route classification tests assert producer routes classify as `RouteAuthMTLS` and the browser observe prefix remains `RouteAuthWebSession`. Cross-user isolation tests prove user A's app certificate cannot overwrite user B's projection and user B's browser cannot read user A's projection. Root-cause fix: the three by-id observe handlers now map `ErrObserve*NotFound` sentinels to 404 instead of 500.
* **Event dashboard classification** — `protocol/constants/event_dashboard_classification.json` updated: `g8e.v1.app.run` reclassified from `unsupported` to `produced_to_sse` (verified by real HTTP integration tests). `g8e.v1.app.agent` description updated to reflect the verified producer (classification remains `mixed` because `app.agent.activity.recorded` is `governed_record_only`). `g8e.v1.ai.eval` remains `unsupported` until the eval publication path lands.
* **Audited g8e-adapter (Packet 7)** — `dashboard/g8e-adapter/` implements runtime config parsing, endpoint allowlist (20 operations), credentialed fetch, WebAuthn ceremonies, SSE normalizer/stream/polling, observe client, five typed stores with pure reducers, safe presentation registry, and explicit honest view states. 366 adapter unit tests pass. The minimal host builds cleanly.
* **Contract pack and reference frontend (Packet 8)** — `dashboard/g8e-adapter/contract-pack/` contains deterministic, generator-neutral inputs: `builder-prompt.md`, `runtime-config.schema.json`, `observe.openapi.json`, `event-schemas.json`, `models.ts`, 11 typed fixtures, and `manifest.json` with SHA-256 of every output. `npm run gen:contract-pack:check` passes (committed outputs are current). The reference frontend at `dashboard/g8e-adapter/reference-ui/` wraps the audited adapter with pure render functions, design-preview fixture isolation, accessibility markers, and honest view states. 48 new tests (16 contract pack + 32 reference UI) pass. Total adapter tests: 414.
* **Full verification** — `./g8e test unit` passes (full Tier 1 suite). `./g8e test integration` passes (full Tier 2 suite). `./g8e test lint` passes (0 issues). Ensemble `make test` passes (4255 passed, 52 skipped). Ensemble `make lint` passes (ruff + pyright clean). Adapter `npm test` passes (414 tests). Adapter `npm run lint` passes. Adapter `npm run build` passes. Adapter `npm run build:host` passes. Adapter `npm run build:ref-ui` passes. Adapter `npm run gen:contract-pack:check` passes.

#### Owner-operated gates (not satisfied by unit tests)

The following gates require owner credentials, real browsers, real authenticators, hosted origins, or platform machines. They are not marked complete from automated tests.

* **Real-browser contract tests** — Exact-origin CORS, WebAuthn test authenticator, session cookies, mTLS producers, observe snapshots, SSE replay/live delivery, and two isolated users against a real HTTPS Gateway fixture.
* **Builder acceptance** — At least one supported builder (Lovable, Notion, or other) consumes the contract pack and produces a deployable SPA that connects through the untouched audited adapter.
* **Real workload projections** — A real g8ee investigation and governed operator workflow produces valid agent and run projections through the mTLS endpoint; the browser read API returns them; the SSE stream carries the nested typed update.
* **Eval publication** — Blocked. The complete `g8e-evals verify <bundle>` prerequisite does not exist. The existing `verify-receipts` command verifies only receipt signatures and is not complete eval verification. No eval publication, projection, events, or downloads are implemented until the verifier lands.
* **Cross-platform trust** — Linux, macOS, and Windows trust installation and browser-restart behavior. Record unsupported/manual trust honestly.
* **Connection** — Exact-origin `gw connect`, CORS, Local Network Access, trust, top-level passkey, cookie restoration, SSE credentials.

#### Documentation

* `docs/guides/build_frontend.md` updated with the generator-neutral observe frontend section, observe API reference, runtime configuration, and acceptance workflow.
* `docs/guides/build_observe_frontend.md` added: generator-neutral builder guide explaining runtime capability requirements.
* `docs/guides/lovable.md` updated with the observe frontend contract pack handoff.
* `docs/architecture/sse.md` updated with observe state producers, first-write validation, ownership and disclosure, and browser read surface.
* `docs/ensemble/sse.md` updated with observe state producers, identity and payload helpers, and lifecycle producer wiring (chat/tool, consensus, investigation, task, operator dispatch).
* `docs/ensemble/evals.md` updated with the browser publication and observe projections section (verifier prerequisite, publication boundary, verification labels, event classification).
* `docs/architecture/protocol.md` updated with observe API models, event dashboard classification, and TypeScript generation.
* `docs/architecture/dashboard.md` and `docs/dashboard/sse.md` updated to reference the audited adapter and keep the in-tree dashboard incompatibility explicit.
