{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MCP Protocol Schemas",
  "description": "Non-normative JSON Schema bundle for g8e MCP payload types extracted from proto definitions.",
  "version": "v1",
  "definitions": {
    "McpCallRequested": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "McpCallRequested",
      "type": "object",
      "description": "Payload for g8e.v1.operator.mcp.call.requested. Carries the downstream MCP tool name and its JSON-RPC arguments verbatim so the Gateway can verify L1 forbidden patterns against the tool name before the Warden dispatches the verified call to the configured downstream MCP server.",
      "properties": {
        "tool_name": {
          "type": "string",
          "description": "The MCP tool name (e.g., 'fs_read', 'drop_table'). Forbidden patterns here block verification at L1 before any signature or state check."
        },
        "arguments_json": {
          "type": "string",
          "description": "JSON-encoded arguments object exactly as the MCP client supplied it. Stored as a string (not Struct) so the canonical hash is computed over the bytes the client signed, with no normalization ambiguity."
        },
        "execution_id": {
          "type": "string",
          "description": "Optional client-supplied invocation id surfaced back in the result."
        }
      },
      "required": ["tool_name", "arguments_json"]
    },
    "McpResourceListRequested": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "McpResourceListRequested",
      "type": "object",
      "description": "Payload for g8e.v1.operator.mcp.resources.list.requested. Lists available resources from the downstream MCP server.",
      "properties": {
        "execution_id": {
          "type": "string",
          "description": "Optional client-supplied invocation id surfaced back in the result."
        }
      }
    },
    "McpResourceReadRequested": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "McpResourceReadRequested",
      "type": "object",
      "description": "Payload for g8e.v1.operator.mcp.resources.read.requested. Reads a specific resource from the downstream MCP server.",
      "properties": {
        "uri": {
          "type": "string",
          "description": "The resource URI to read (e.g., 'file:///path/to/file', 'memory://var')."
        },
        "execution_id": {
          "type": "string",
          "description": "Optional client-supplied invocation id surfaced back in the result."
        }
      },
      "required": ["uri"]
    },
    "McpPromptListRequested": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "McpPromptListRequested",
      "type": "object",
      "description": "Payload for g8e.v1.operator.mcp.prompts.list.requested. Lists available prompts from the downstream MCP server.",
      "properties": {
        "execution_id": {
          "type": "string",
          "description": "Optional client-supplied invocation id surfaced back in the result."
        }
      }
    },
    "McpPromptGetRequested": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "McpPromptGetRequested",
      "type": "object",
      "description": "Payload for g8e.v1.operator.mcp.prompts.get.requested. Gets a specific prompt template from the downstream MCP server.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The prompt name/template identifier."
        },
        "execution_id": {
          "type": "string",
          "description": "Optional client-supplied invocation id surfaced back in the result."
        }
      },
      "required": ["name"]
    }
  }
}
