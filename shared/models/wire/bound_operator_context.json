{
  "_comment": "Canonical wire format for bound operator context carried in X-G8E-Bound-Operators header from g8ed to G8EE. This model ensures wire compatibility and enforces consistency across components.",

  "_comment.field_naming": "FIELD NAMING: Wire format models use 'operator_id' instead of 'id' for clarity in cross-component contexts where the entity type may not be immediately obvious. This maps directly to OperatorDocument.core_identity.id (see operator_document.json _comment.field_naming for the full convention).",

  "bound_operator_context": {
    "_description": "Single bound operator entry in the X-G8E-Bound-Operators header array. g8ed resolves this from the database (BoundSessionsDocument + operator docs) and g8ee parses it from the HTTP header.",
    "fields": {
      "operator_id": {
        "type": "string",
        "required": true,
        "description": "Durable operator identifier. Maps from OperatorDocument.core_identity.id."
      },
      "operator_session_id": {
        "type": "string",
        "required": false,
        "description": "Operator session identifier for this binding"
      },
      "bound_web_session_id": {
        "type": "string",
        "required": false,
        "description": "Web session ID this operator is bound to (for binding validation)"
      },
      "status": {
        "type": "string",
        "required": false,
        "description": "Operator status (e.g., 'bound', 'active', 'available'). Consumers should validate against their own enum values."
      }
    }
  }
}
