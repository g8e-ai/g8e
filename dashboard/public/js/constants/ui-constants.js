// Copyright (c) 2026 Lateralus Labs, LLC.
// Licensed under the Business Source License 1.1 — see LICENSE for details.

export const CssClass = Object.freeze({
    INITIALLY_HIDDEN:         'initially-hidden',
    VISIBLE:                  'visible',
    MSG_SENDING:              'sending',
    MSG_FAILED:               'failed',
    MSG_WAITING_RESPONSE:     'waiting-response',
});

export const WheelDelta = Object.freeze({
    LINE_HEIGHT_PX: 16,
});

export const CopyText = Object.freeze({
    PARAGRAPH_BREAK: '\n\n',
});
