# Copyright (c) 2026 Lateralus Labs, LLC.
# Licensed under the Apache License, Version 2.0
# Auto-generated by ./g8e docker

services:
  gateway0:
    build:
      context: ..
      dockerfile: docker/Dockerfile.gateway
    image: g8e-gateway:latest
    container_name: g8e-gateway0
    hostname: gateway0
    networks:
      - g8e-network
    ports:
      - "9000:9000"  # HTTPS mTLS API
      - "9001:9001"  # Bootstrap TLS (CSR enrollment)
      - "9002:9002"  # Public browser/BYO bootstrap
    volumes:
      - gateway0-data:/home/g8eg/.g8e/data
      - gateway0-pki:/home/g8eg/.g8e/pki
      - gateway0-secrets:/home/g8eg/.g8e/secrets
    healthcheck:
      test: ["CMD", "/home/g8eg/g8e-linux-amd64", "gw", "status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    restart: unless-stopped

networks:
  g8e-network:
    driver: bridge

volumes:
  gateway0-data:
    driver: local
  gateway0-pki:
    driver: local
  gateway0-secrets:
    driver: local
