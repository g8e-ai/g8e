Surgical find-and-replace on a file.

<parameters>
<required>
- file_path: Absolute path to the file
- old_content: Exact text to find (must match perfectly including whitespace)
- new_content: Replacement text
- justification: Explanation for why this update is needed
</required>
<optional>
- create_backup: Backup before modifying (default: true)
</optional>
</parameters>

<critical>
old_content MUST match file content exactly, including whitespace.
Always READ the file first and copy the exact text to replace.
Fails safely if old_content is not found.
</critical>

<examples>
<example>User: "Change nginx port to 8080" → file_update_on_operator(file_path="/etc/nginx/nginx.conf", old_content="listen 80;", new_content="listen 8080;", justification="Update nginx port")</example>
<example>User: "Fix typo in app.py" → file_update_on_operator(file_path="/app/app.py", old_content="prnt('hello')", new_content="print('hello')", justification="Fix typo in print statement")</example>
</examples>
